package zero

modules = Main
main = Main

depends = contrib
        , network

executable = zero

sourcedir = "src"

prebuild = "make -C src && cp src/*.so build/exec/zero_app"
postclean = "make -C src clean"